<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.iwuvhugs.wallty.LaunchActivity">


    <Button
        android:id="@+id/sign_in"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_above="@+id/unsigned_user"
        android:layout_marginBottom="8dp"
        android:text="@string/launch_button_login"
        android:textSize="16sp"></Button>

    <Button
        android:id="@+id/unsigned_user"
        android:layout_width="match_parent"
        android:layout_height="48dp"
        android:layout_above="@+id/discover"
        android:layout_marginBottom="8dp"
        android:text="@string/launch_button_guest"
        android:textSize="16sp"></Button>

    <TextView
        android:id="@+id/discover"
        android:layout_width="wrap_content"
        android:layout_height="32dp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="24dp"
        android:gravity="center"
        android:text="@string/launch_button_discover"
        android:textSize="16sp" />

</RelativeLayout>